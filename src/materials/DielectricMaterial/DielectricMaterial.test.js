/* eslint-env mocha */
const { expect } = require('chai')
const DielectriMaterial = require('.')

describe('DielectricMaterial', () => {
  describe('scatter()', () => {
    it('returns a scattered ray and an attenuation value')
  })
})
